<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whatsapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/msg"
        android:label="@string/app_name"
        android:roundIcon="@drawable/msg"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GroupMembre"></activity>
        <activity android:name=".ParametreActivity" />
        <activity android:name=".GroupChatActivity" />
        <activity android:name=".Home" />
        <activity android:name=".FirebaseUser" />
        <activity android:name=".ActivityUser" />
        <activity android:name=".Inscription" />
        <activity android:name=".Connexion" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>